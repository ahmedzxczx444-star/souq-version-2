export const subscriptionsEnabled = false; // FEATURE FLAG: Set to true to enable subscription system

// FEATURE FLAGS: dealer category dashboards (multi-branch/chain/importer/official-agent)
// and the auto-parts marketplace ship dark until each is verified in production.
export const dealerCategoriesEnabled = true;
export const partsMarketplaceEnabled = true;
